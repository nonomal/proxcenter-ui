# OS files
.DS_Store
Thumbs.db
*_Zone.Identifier
*Zone.Identifier

# Editor files
*.swp
*.swo
*~
.idea/
.vscode/

# Claude artifacts
.claude/
AGENTS.md
CLAUDE.md

# Environment files
.env
.env.local
.env.*.local

# IDE
*.iml

# Next.js build
.next/

# Root-level caches from dev tooling that runs at the repo root rather
# than under frontend/ (vitest's vite cache landed here once via a stray
# install).
/node_modules/

# Lock files
package-lock.json

# Docker
config/orchestrator.yaml
!config/orchestrator.yaml.example
config/prometheus.yml
# Per-developer compose override (port mappings, volume binds, etc.)
docker-compose.override.yml

# Data directories
data/
*.db
*.db-journal
backend/
.gitnexus
.worktrees/

# Internal planning + design docs — must NEVER land on the public repo.
# Convention going forward: anything strategic (phased plans, migration
# audits, comp analysis, internal architecture decisions) lives outside
# this repo or in a private one. The Superpowers skill writes its plans
# and specs under docs/superpowers/; that path is now ignored entirely.
**/docs/superpowers/
**/docs/internal/
**/docs/audits/
**/docs/plans/
**/docs/specs/
docs/postgres-migration-inventory.md
